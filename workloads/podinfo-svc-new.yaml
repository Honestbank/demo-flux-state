apiVersion: v1
kind: Service
metadata:
  name: new-podinfo
  labels:
    app: new-podinfo
spec:
  type: ClusterIP
  selector:
    app: new-podinfo
  ports:
    - name: http
      port: 9898
      protocol: TCP
      targetPort: http
